// postcss.config.mjs
export default {
  plugins: {
    // Tailwind’s official PostCSS plugin (v4+)
    '@tailwindcss/postcss': {},
    // autoprefixer is optional but recommended
    autoprefixer: {},
  },
}
